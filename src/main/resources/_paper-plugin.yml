name: ${name}
main: ${main}
version: ${version}
author: ${author}
contributors: [Dominilk]
api-version: "1.19"

dependencies:
  server:
    packetevents:
      load: BEFORE
      required: true
      join-classpath: true
